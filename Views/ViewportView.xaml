<UserControl x:Class="StudioForge.Views.ViewportView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:StudioForge.Views">
    <Border Background="{StaticResource Brush.PanelBackground}" BorderBrush="{StaticResource Brush.PanelBorder}" BorderThickness="0,0,1,0">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="32" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Background="{StaticResource Brush.PanelBackground}" BorderBrush="{StaticResource Brush.PanelBorder}" BorderThickness="0,0,0,1" Padding="8,4">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <Button Content="Test ▼" Style="{StaticResource ToolbarButtonStyle}" Padding="10,4" />
                    <Button Content="⊞ 1 studs ▼" Style="{StaticResource ToolbarButtonStyle}" Padding="10,4" />
                </StackPanel>
            </Border>

            <Grid Grid.Row="1" Background="{StaticResource Brush.WindowBackground}">
                <local:ViewportCanvas />
            </Grid>
        </Grid>
    </Border>
</UserControl>
