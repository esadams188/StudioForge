<UserControl x:Class="StudioForge.Views.OutputView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Border Background="{StaticResource Brush.PanelBackground}" BorderBrush="{StaticResource Brush.PanelBorder}" BorderThickness="0,1,0,0">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="32" />
                <RowDefinition Height="*" />
                <RowDefinition Height="36" />
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Background="{StaticResource Brush.PanelBackground}" BorderBrush="{StaticResource Brush.PanelBorder}" BorderThickness="0,0,0,1" Padding="8,6">
                <TextBlock Text="Output" Style="{StaticResource PanelHeaderText}" />
            </Border>

            <ListBox Grid.Row="1" ItemsSource="{Binding Logs}" Style="{StaticResource OutputListBoxStyle}">
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem" BasedOn="{StaticResource OutputListItemStyle}" />
                </ListBox.ItemContainerStyle>
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="100" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="80" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="{Binding Time}" Foreground="{StaticResource Brush.TextMuted}" />
                            <TextBlock Grid.Column="1" Text="{Binding Message}" Foreground="{Binding Color}" />
                            <TextBlock Grid.Column="2" Text="{Binding Category}" Foreground="{StaticResource Brush.TextMuted}" HorizontalAlignment="Right" />
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

            <Grid Grid.Row="2" Background="{StaticResource Brush.PanelBackground}" BorderBrush="{StaticResource Brush.PanelBorder}" BorderThickness="0,1,0,0" Padding="8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="120" />
                    <ColumnDefinition Width="120" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <ComboBox SelectedIndex="0" Grid.Column="0" Background="#2B2B2B" Foreground="{StaticResource Brush.TextPrimary}" BorderBrush="{StaticResource Brush.PanelBorder}" ItemsSource="{Binding OutputFilterOptions}" />
                <ComboBox SelectedIndex="0" Grid.Column="1" Background="#2B2B2B" Foreground="{StaticResource Brush.TextPrimary}" BorderBrush="{StaticResource Brush.PanelBorder}" ItemsSource="{Binding OutputContextOptions}" />
                <TextBox Grid.Column="2" Margin="8,0,0,0" Background="#2B2B2B" Foreground="{StaticResource Brush.TextPrimary}" BorderBrush="{StaticResource Brush.PanelBorder}" Padding="6,2">
                    <TextBox.Style>
                        <Style TargetType="TextBox">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="TextBox">
                                        <Grid>
                                            <ScrollViewer Margin="0" x:Name="PART_ContentHost" />
                                            <TextBlock x:Name="Watermark" Text="Filter..." Foreground="{StaticResource Brush.TextMuted}" IsHitTestVisible="False" Margin="6,2" Visibility="Hidden" />
                                        </Grid>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="Text" Value="">
                                                <Setter TargetName="Watermark" Property="Visibility" Value="Visible" />
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </TextBox.Style>
                </TextBox>
            </Grid>
        </Grid>
    </Border>
</UserControl>
